# Skill Registry — simulador-exil

Greenfield project. No project-specific skills defined yet. Global/plugin skills apply
(SDD phases, work-unit-commits, chained-pr, branch-pr, code-review, etc.).

## Compact Rules

_No project-specific rules yet._ When the stack is scaffolded (Vite + TypeScript + React,
hexagonal architecture), add compact rules here for:

- Domain purity (no UI/infra imports in `domain/`).
- Ports & Adapters boundaries (`ContentPort`, `StoragePort`).
- Vitest + React Testing Library conventions.
- Container-presentational + atomic design for components.

## User Skills

_None registered for this project._
